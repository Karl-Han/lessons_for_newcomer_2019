#include "pointer.h"

int main() {
    // random_int_arr();
    // for (int i = 0; i < 10; i++) {
    //    printf("fib(%d) = %d\n", i, fibonacci(i));
    //}
    // pointer_alone();
    pointer_and_array();
    return 0;
}
